[package]
name = "engine"
version = "0.3.0"
edition = "2024"
publish = false

[dependencies]
rand = "0.10"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
