[package]
name = "domain"
version = "0.3.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
